# TRADEOFFS

설계 과정에서 내린 선택과 그 근거, 감수한 한계를 기록합니다.

## 1. Vercel Serverless Function 프록시로 API 호출

- **선택**: 브라우저가 OpenWeather를 직접 호출하지 않고 `/api/weather`·`/api/forecast` 서버리스 함수를 거친다. API 키는 `VITE_` 접두사 없는 서버 전용 환경 변수로만 존재하므로 번들에 포함될 수 없다. 로컬 개발에서는 Vite dev 서버의 proxy가 같은 역할을 대신한다.
- **이유**: 클라이언트 직접 호출은 배포된 번들에서 키를 추출할 수 있다(`VITE_` 변수는 빌드 시점에 코드에 치환된다). 배포 대상인 Vercel이 함수를 기본 제공하므로 별도 서버 없이 키를 서버 측으로 옮길 수 있고, 엣지 캐시(`s-maxage`)로 무료 호출 한도 보호가 한 층 늘어난다.
- **한계**: 요청이 프록시를 한 번 더 거쳐 응답이 소폭 느려진다. 로컬 개발이 Vite proxy 설정에 의존하고, Vercel 외 호스팅으로 옮기면 함수를 다시 구현해야 한다.

## 2. localStorage 캐시 (현재 10분 · 예보 30분)

- **선택**: TTL 기반 localStorage 캐시. 어댑터 형태가 바뀌면 키 버전을 올려 무효화(`current:v2:`).
- **이유**: 무료 한도(분당 60회)에서 10개 도시 병렬 호출 + 새로고침이 겹치면 429가 난다. 예보는 현재 날씨보다 변화가 느려 TTL을 다르게 설정.
- **한계**: TTL 이내의 급격한 날씨 변화는 반영되지 않는다. "다시 시도" 버튼도 캐시 히트 시 네트워크를 타지 않는다.

## 3. 전역 시계 타이머는 해제하지 않음

- **선택**: `useNow`의 `setInterval`(1분 주기)은 모듈 레벨에 하나만 만들고 `clearInterval` 하지 않는다.
- **이유**: 현지 시각은 앱 전체 수명 동안 필요한 전역 상태다. 컴포넌트마다 타이머를 만들면 10개가 제각각 돌고, 정리 누락 시 누수가 된다. 하나로 공유하면 정리할 필요 자체가 없다.
- **한계**: 백그라운드 탭에서 타이머가 억제된다 — `visibilitychange`에서 즉시 갱신하는 것으로 보완했다.

## 4. ApexCharts로 인한 상세 청크 비대 (~850kB, gzip 241kB)

- **선택**: 차트 라이브러리를 전역 등록하지 않고 `ForecastChart` 안에서만 import해 상세 페이지 청크에 격리.
- **이유**: 홈 첫 화면 번들(60kB)을 지키는 것이 우선. 차트는 상세 진입 시에만 필요하다.
- **한계**: 상세 첫 진입이 무겁다. 더 줄이려면 경량 차트(uPlot 등)로 교체하거나 SVG를 직접 그려야 한다.

## 5. history 라우팅 유지 + Vercel rewrite

- **선택**: `createWebHistory`를 유지하고, `vercel.json`의 rewrite로 모든 경로를 `index.html`에 돌린다.
- **이유**: 해시 라우팅(`#/about`)은 서버 설정이 불가능한 정적 호스팅의 우회책이다. Vercel은 rewrite를 지원하므로 깨끗한 URL을 유지할 수 있다.
- **한계**: rewrite를 지원하지 않는 호스팅(GitHub Pages 등)으로 옮기면 새로고침 404가 재발한다 — 그 경우 해시 모드로 전환해야 한다.

## 6. 시간대 판정: 일출·일몰 앵커 + 순차 임계값

- **선택**: 고정 시각(6시/18시)이 아니라 API의 `sunrise`/`sunset`을 앵커로 새벽·아침·낮·노을·밤을 판정한다. 임계값은 위에서부터 순서대로 검사한다.
- **이유**: 고정 시각은 여름 런던(21시 일몰)과 겨울 서울(17시 일몰)을 구분하지 못한다. 순차 검사는 극단 위도에서 구간이 겹칠 때도 잘못된 값 대신 구간 축소로 강등된다.
- **한계**: 극야·백야 지역에서는 일부 구간이 아예 나타나지 않는다. 대상 10개 도시 범위에서는 문제가 없다.

## 7. 절차적 CSS 배경 (사진·영상 대신)

- **선택**: 태양·달·별·구름·그레인을 전부 CSS/SVG로 생성.
- **이유**: 용량 0에 가깝고, 라이선스 문제가 없고, 네트워크·심사 환경과 무관하게 동작한다. 시간대 크로스페이드(그라디언트 5장 + opacity)와 자연스럽게 결합된다.
- **한계**: 사진의 사실감에는 미치지 못한다.

## 8. 커스텀 디자인 시스템 + 라이브러리 국소 사용

- **선택**: 핵심 UI(카드·검색·탭·토글)는 디자인 토큰 기반으로 직접 만들고, 라이브러리는 잘하는 곳에만 쓴다 — 차트 ApexCharts, 스켈레톤 Naive UI, 아이콘 Lucide/Meteocons.
- **이유**: 유리 + 시간대 색이라는 고유 컨셉을 컴포넌트 라이브러리 테마 위에서 구현하면 오버라이드 싸움이 된다.
- **한계**: 접근성·엣지 케이스를 직접 책임져야 한다 (키보드 포커스, aria 속성 등을 수동으로 처리했다).

## 9. Element Plus 의존성 유지

- **선택**: 본편에서 쓰지 않는 Element Plus를 제거하지 않고, `/practices` 라우트(수업 실습·Code Challenge)의 의존성으로 유지한다.
- **이유**: 실습 결과물은 학습 이력이며 과제 제출물의 일부다. `unplugin-vue-components` 트리셰이킹으로 본편 번들에는 포함되지 않는다.
- **한계**: `node_modules`와 설치 시간이 늘어난다.

## 10. Pretendard 웹폰트 CDN 로드

- **선택**: 시스템 폰트에만 의존하지 않고 Pretendard Variable을 jsDelivr CDN에서 로드한다.
- **이유**: 온도 숫자의 얇은 웨이트(200)가 디자인 시그니처인데, Windows의 맑은 고딕에는 얇은 웨이트가 없어 렌더링이 무너진다. 가변 폰트 + dynamic subset이라 필요한 글리프만 내려받는다.
- **한계**: 외부 CDN 의존이 생긴다. CDN 장애 시에는 폰트 스택의 시스템 폰트로 자연스럽게 강등된다.
